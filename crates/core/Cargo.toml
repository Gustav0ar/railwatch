[package]
name = "railwatch-core"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-or-later"
[dependencies]
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.10"
iana-time-zone = "0.1"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
